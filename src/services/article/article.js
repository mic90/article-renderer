export default class Article {
  constructor(image, heading, author, timestamp, body) {
    this.image = image;
    this.heading = heading;
    this.author = author;
    this.timestamp = timestamp;
    this.body = body;
  }
}
